#ifndef ITESOL_BACKENDS_EIGEN_DENSE_HPP
#define ITESOL_BACKENDS_EIGEN_DENSE_HPP

#include <Eigen/Dense>

namespace itesol {
    namespace backends {

        template <typename T>
        class EigenDenseBackend {};
    } // namespace backends
} // namespace itesol

#endif // ITESOL_BACKENDS_EIGEN_DENSE_HPP
